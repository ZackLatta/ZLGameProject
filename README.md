# ZLGameProject
-Finished a beta title screen
-bat moves across the screen, loops near the center, then continues across the screen until it goes offscreen, then goes back to original position. can kill the bat by pressing 'k'
-title screen moves up and down slightly
-3 objects (1 torah gate and 2 soldiers) are positioned near the center of the screen
-can use backspace or escape to exit
